{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "C/C++: clang++ build active file",
			"command": "/usr/bin/clang++",
			"args": [
				"-g",
				"-std=c++17",
				"${file}",
				"-lpqxx",
				"-lpq",
				"-lboost_system",
				"-lboost_filesystem",
				"-o",
				"${fileDirname}/${fileBasenameNoExtension}-mac"
			],
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"problemMatcher": [
				"$gcc"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}